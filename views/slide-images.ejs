<md-content class="main-content" ng-controller="SlideImageCtrl">

  <md-table-toolbar>
    <div layout="row" layout-align="center center" class="padding">
      <span flex></span>
      <md-button class="md-primary md-raised" md-colors="::{ color: 'white' }" aria-label="New"
        ng-click="onEdit($event)" translate="CREATE" translate-cloak="CREATE">
      </md-button>
    </div>
  </md-table-toolbar>

  <md-table-container>
    <table md-table md-progress="promise">
      <thead md-head md-on-reorder="onReorder">
        <tr md-row>
          <th md-column class="text-center text-uppercase bold">
            <span translate="IMAGE" translate-cloak="IMAGE"></span>
          </th>
          <th md-column class="text-center text-uppercase bold" md-order-by="sort">
            <md-icon></md-icon>
            <span translate="SORT" translate-cloak="SORT"></span>
          </th>
          <th md-column class="text-center text-uppercase bold">
            <span translate="LINK_TO" translate-cloak="LINK_TO"></span>
          </th>
          <th md-column class="text-center text-uppercase bold">
            <span translate="STATUS" translate-cloak="STATUS"></span>
          </th>
          <th md-column></th>
        </tr>
      </thead>
      <tbody md-body>
        <tr md-row ng-repeat="slide in slides">
          <td md-cell class="text-center">
            <img width="80" ng-src="/images/placeholder.png" actual-src="{{:: slide.imageThumb.url() }}" />
          </td>
          <td md-cell class="text-center">{{:: slide.sort }}</td>
          <td md-cell class="text-center">
            <span ng-if="slide.type === 'url'">
              {{:: slide.url }}
            </span>
            <span ng-if="slide.type === 'brand'">
              {{:: slide.brand.name }}
            </span>
            <span ng-if="slide.type === 'category'">
              {{:: slide.category.name }}
            </span>
            <span ng-if="slide.type === 'subcategory'">
              {{:: slide.subcategory.name }}
            </span>
            <span ng-if="slide.type === 'item'">
              {{:: slide.item.name }}
            </span>
          </td>
          <td md-cell class="text-center">
            <span class="label" ng-class="::{ 'green': slide.isActive,
						  'yellow': !slide.isActive }">
              <span ng-if="slide.isActive" translate="ACTIVE" translate-cloak="ACTIVE"></span>
              <span ng-if="!slide.isActive" translate="INACTIVE" translate-cloak="INACTIVE"></span>
            </span>
          </td>
          <td md-cell>
            <section layout="row" layout-align="center center">
              <md-button class="md-icon-button md-primary" ng-click="onEdit($event, slide)" aria-label="Edit" flex="35">
                <ng-md-icon icon="mode_edit"></ng-md-icon>
              </md-button>
              <md-button class="md-icon-button md-primary" ng-click="onDelete($event, slide)" aria-label="Delete"
                flex="35">
                <ng-md-icon icon="clear"></ng-md-icon>
              </md-button>
            </section>
          </td>
        </tr>
      </tbody>
    </table>
  </md-table-container>

</md-content>